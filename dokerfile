FROM php:7.4-fpm

WORKDIR /var/www/html

RUN apt-get update && \
    apt-get install -y \
    unzip \
    libpq-dev \
    && docker-php-ext-install pdo_pgsql

COPY . .

CMD ["php", "bin/console", "server:run", "0.0.0.0:8000"]